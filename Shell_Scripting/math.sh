#!/bin/bash
#math examples

a=30 b=15
echo `expr $a + $b`
echo `expr $a - $b`
echo `expr $a \* $b`
echo `expr $a / $b`
echo `expr $a % $b`

#reverse quotes to get shell to not just print out the statement but do what we want
